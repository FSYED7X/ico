const colors = {
  yellow: {
    variant1: "#f5dc7d", //! countdown bg, stage 1 bg,
    variant2: "#ffcc33", //! countdown border, slider, buy button, max input andornment
    variant3: "#e8b32e", //! starts in bg, token contract address text,
  },
};

export default colors;
